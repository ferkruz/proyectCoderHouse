import { ResaltarCursosDirective } from './resaltar-cursos.directive';

describe('ResaltarCursosDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarCursosDirective();
    expect(directive).toBeTruthy();
  });
});
