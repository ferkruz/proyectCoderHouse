import { RoundBlockCursosDirective } from './round-block-cursos.directive';

describe('RoundBlockCursosDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundBlockCursosDirective();
    expect(directive).toBeTruthy();
  });
});
